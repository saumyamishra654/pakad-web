# The frontend is hosted separately (Vercel); nginx here fronts only the API.
server {
    listen 80;
    server_name _;
    client_max_body_size 500M;

    # ACME http-01 challenges for certbot.
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /api/ {
        proxy_pass http://api:8765;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Long timeout for pipeline jobs.
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }

    location / {
        return 404;
    }
}

# --- TLS server: enable after issuing certs. Replace DOMAIN and server_name,
# --- then uncomment. (nginx does not expand env vars in plain configs.)
# server {
#     listen 443 ssl;
#     server_name api.yourdomain.com;
#     client_max_body_size 500M;
#
#     ssl_certificate     /etc/letsencrypt/live/DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/DOMAIN/privkey.pem;
#
#     location /api/ {
#         proxy_pass http://api:8765;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 600s;
#         proxy_send_timeout 600s;
#     }
#     location / { return 404; }
# }
