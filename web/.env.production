# API base for the deployed backend (empty = same-origin; set for Vercel split hosting)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# NEXT_PUBLIC_FIREBASE_* — set in the Vercel project env, not here
