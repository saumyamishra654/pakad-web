# Backend API (FastAPI + Firebase)
firebase-admin>=6.0.0
google-cloud-firestore>=2.11.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Pipeline core (driver.py / raga_pipeline)
numpy>=2.0.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
librosa>=0.10.0
matplotlib>=3.8.0
soundfile>=0.12.0

# Stem separation (default: --separator demucs)
torch>=2.0.0
torchaudio>=2.0.0
demucs>=4.0.0

# Pitch extraction (default: --pitch-extractor swiftf0)
swift-f0>=0.1.2

# YouTube uploads / preprocess ingest
yt-dlp>=2024.1.0

# System deps (install separately, not via pip):
#   ffmpeg, ffprobe — audio conversion, stem encoding, duration checks
# Optional:
#   spleeter>=2.4.0  — only if using --separator spleeter instead of demucs
